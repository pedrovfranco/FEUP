package recipes;

import java.util.ArrayList;
import java.util.List;

public class RecipeStep
{
	private String name;
	private String action;
	
	private List< Pair<SimpleIngredient, Float> > ingredients = new ArrayList< Pair<SimpleIngredient, Float> >();
	
	public RecipeStep(String name, String action)
	{
		if (name == null || action == null)
			throw new IllegalArgumentException();
		
		this.name = name;
		this.action = action;
	}
	
	public String getName()
	{
		return name;
	}
	
	public String getAction()
	{
		return action;
	}
	
	public void addIngredient(SimpleIngredient input, float quantity)
	{
		Pair<SimpleIngredient, Float> foo = new Pair<SimpleIngredient, Float>(input, quantity);
		if (!ingredients.contains(foo))
		{
			ingredients.add(foo);
		}
			
	}
	
	public Float getQuantity(SimpleIngredient input)
	{
		for (int i = 0; i < ingredients.size(); i++)
		{
			if (ingredients.get(i).getFirst().equals(input))
				return ingredients.get(i).getSecond();
		}
		
		return 0F;
	}
	
	public int getIngredientCount()
	{
		return ingredients.size();
	}
}
